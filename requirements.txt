# LMT Defence - Python Dependencies
# FastAPI and uvicorn
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# HTTP client for testing (required by FastAPI TestClient)
httpx>=0.25.0

# Testing
pytest>=7.0.0

# Database (SQLite is built-in, no external driver needed)
